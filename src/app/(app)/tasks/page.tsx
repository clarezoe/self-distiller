import { Placeholder } from "@/components/placeholder";

export default function TasksPage() {
  return (
    <Placeholder
      title="Tasks"
      note="Use the trained Self Model to draft replies, copy, scripts, and emails in the right context. Draft Mode only."
    />
  );
}
