import { Placeholder } from "@/components/placeholder";

export default function SelfModelPage() {
  return (
    <Placeholder
      title="Self Model"
      note="View core self, language/role/relationship/scene models, evidence links, confidence, and boundaries. Export to Markdown/JSON."
    />
  );
}
