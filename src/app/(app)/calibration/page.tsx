import { Placeholder } from "@/components/placeholder";

export default function CalibrationPage() {
  return (
    <Placeholder
      title="Calibration"
      note="Blind calibration: the agent predicts a hidden reply, you write your real one, and the system compares to correct the model."
    />
  );
}
